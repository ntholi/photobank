export const APP_NAME = 'PhotoBank';

export const constants = {
    UPLOAD_FOLDER: 'uploads',
};

export enum PhotoType {
    UPLOADS = 'Uploads',
    PURCHASED = 'Purchased',
    SAVED = 'Saved',
}
